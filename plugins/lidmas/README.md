# LiDMaS+ Plugin Placeholder

This directory is reserved for a future LiDMaS+ decoder integration.

Expected responsibilities:

- Accept detector measurements or normalized result records.
- Run decoding workflows.
- Return decoded logical information, diagnostics, and confidence metadata.
- Keep decoder-specific dependencies optional.

The core package currently exposes `LiDMaSPlugin` as a placeholder class and
does not import a real LiDMaS+ package.
