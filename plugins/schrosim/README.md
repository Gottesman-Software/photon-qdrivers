# SchroSIM Plugin Placeholder

This directory is reserved for a future SchroSIM integration.

Expected responsibilities:

- Translate Photon-QDrivers symbolic circuits into SchroSIM-compatible input.
- Run local or remote simulation jobs.
- Normalize simulator output into Photon-QDrivers result dictionaries.
- Report simulator capabilities and version metadata.

The core package currently exposes `SchroSIMPlugin` as a placeholder class and
does not import a real SchroSIM package.
